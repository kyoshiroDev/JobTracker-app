import { StatusColorTextDirective } from './status-color-text.directive';

describe('StatusColorTextDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusColorTextDirective();
    expect(directive).toBeTruthy();
  });
});
